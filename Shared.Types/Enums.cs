namespace Shared.Types
{
        public enum UserType
        {
            Admin = 1,
            Student = 2,
            Teacher = 3
        }

        public enum JobPositionType
        {
            Practice = 1,
            Theory = 2,
        }

        public enum UserStatus
        {
            Active = 1,
            Inactive = 2,
            Deleted = 3
        }
}
